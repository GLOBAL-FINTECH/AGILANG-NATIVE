fn main() -> i32:
    let values = [1.0, 2.0, 3.0]
    let result = values
    print("first", values[0])
    print(result[0].to_string())
    let ok: bool = values[0] < values[1] and values[2] >= 3.0
    return 0
